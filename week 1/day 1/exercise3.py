#Predict the output of the following code snippets:
#Comment what is your guess, then run the code and compare
15 < 8 #False
"15" < "8" #True, because it compares the first character of each string, and "1" is less than "8"
# --- IGNORE ---
"3">"7" #False, because it compares the first character of each string, and "3" is greater than "7"
